version: "3.8"

services:
  # # Shouldn't be recognized by ccu because of invalid semver
  # nginx-latest:
  #   image: nginx:latest

  # # Shouldn't be recognized by ccu because of invalid semver
  # pytorch-latest:
  #   image: pytorch/pytorch:latest

  # nginx:
  #   image: nginx:1.26.2

  # redis:
  #   image: redis:7.0.5

  postgres:
    image: postgres:14.5

  alpine:
    image: alpine:3.16

  traefik:
    image: traefik:v2.9.3
  # pytorch:
  #   image: pytorch/pytorch:1.8.1-cuda11.1-cudnn8-runtime
